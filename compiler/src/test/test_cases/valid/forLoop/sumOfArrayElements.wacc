# Function that calculates the sum of an array of integers

# Output:
# The sum of the elements in the array is 65

# Exit:
# 0

# Program

begin
  int sumOfArray(int[] array) is
    int total = 0;
    for (int i = 0, i < len array, i = i + 1) do
      total = total + array[i]
    done;
    return total
  end
  int[] arr = [1, 5, 7, 9, 15, 28];
  int result = call sumOfArray(arr);
  print "The sum of the elements in the array is ";
  println result
end
