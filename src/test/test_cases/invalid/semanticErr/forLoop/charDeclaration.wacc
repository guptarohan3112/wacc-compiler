# Looping variable is a character and not an integer

# Output:
# #semantic_error#

# Exit:
# 200

# Program:

begin
  for (char c = 'a', c <= 'b') do
    println "No integer declaration here"
  done
end