image: alexusher/wacc:latest

stages:
    - build
    - testlocal
    - test
    - testExecution

build:
    stage: build
    script:
        - make clean && make

testlocal:
    script:
        - make
        - ./gradlew semanticTests
    only:
        - master

test:
    script:
        - make
        - ./gradlew errorTests
    only:
        - master

testExecution:
    script:
        - make
        - ./gradlew executionTests
    only:
        - master

